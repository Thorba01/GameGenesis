<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="GameGenesisApp.Views.LoginPage"
             xmlns:viewModels="clr-namespace:GameGenesisApp.ViewModels;assembly=GameGenesisApp"
             Shell.NavBarIsVisible="False">


    <ContentPage.BindingContext>
        <viewModels:LoginViewModel/>
    </ContentPage.BindingContext>

    <StackLayout Padding="10,0,10,0" VerticalOptions="Center">
        <ActivityIndicator IsRunning="{Binding IsBusy, Mode=TwoWay}" IsVisible="{Binding IsBusy, Mode=TwoWay}" Color="Black" />
        <Image Source="Assets/logo.png"/>
        <Entry Text="{Binding Email}"  Placeholder="Email" />
        <Entry Text="{Binding Password}" Placeholder="Password" IsPassword="True" />
        <Button Text="Login" Command="{Binding LoginCommand}" HorizontalOptions="FillAndExpand" ></Button>
        <Button Text="Register" Command="{Binding RegisterCommand}" HorizontalOptions="FillAndExpand"></Button>
    </StackLayout>
    
</ContentPage>